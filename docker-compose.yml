services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: jobpulse
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - jobpulse_data:/var/lib/postgresql/data

volumes:
  jobpulse_data:
